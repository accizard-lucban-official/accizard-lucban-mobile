<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- Header with Profile Icon Only -->
    <LinearLayout
        android:id="@+id/header_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="@dimen/padding_large"
        android:paddingEnd="@dimen/padding_large"
        android:paddingTop="@dimen/padding_large"
        android:paddingBottom="@dimen/padding_large"
        android:background="@android:color/white"
        android:elevation="@dimen/card_elevation_medium"
        android:gravity="center_vertical|end"
        android:minHeight="70dp">

        <ImageView
            android:id="@+id/profile_icon"
            android:layout_width="@dimen/profile_picture_small"
            android:layout_height="@dimen/profile_picture_small"
            android:background="@null"
            android:src="@drawable/ic_person"
            android:padding="0dp"
            android:clickable="true"
            android:focusable="true"
            android:scaleType="fitCenter" />

    </LinearLayout>

    <!-- Alerts Header and Filter Dropdowns Row -->
    <LinearLayout
        android:id="@+id/alerts_header_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/header_layout"
        android:orientation="vertical"
        android:paddingStart="@dimen/padding_medium"
        android:paddingEnd="@dimen/padding_medium"
        android:paddingTop="@dimen/padding_medium"
        android:paddingBottom="@dimen/padding_medium"
        android:background="@android:color/white">

        <!-- Alerts Text Header (Left Aligned) -->
        <TextView
            android:id="@+id/alerts_header_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Alerts"
            android:textSize="@dimen/text_size_title"
            android:textColor="#333333"
            android:textStyle="bold"
            android:gravity="start"
            android:layout_marginBottom="@dimen/margin_medium" />

        <!-- Filter Dropdowns Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical|end">

            <!-- Type Filter Spinner -->
            <Spinner
                android:id="@+id/filter_spinner"
                android:layout_width="0dp"
                android:layout_height="@dimen/button_height_medium"
                android:layout_weight="1"
                android:layout_marginEnd="@dimen/margin_small"
                android:minWidth="50dp"
                android:background="@drawable/spinner_with_outline" />

            <!-- Priority Filter Spinner -->
            <Spinner
                android:id="@+id/priority_filter_spinner"
                android:layout_width="0dp"
                android:layout_height="@dimen/button_height_medium"
                android:layout_weight="1"
                android:minWidth="50dp"
                android:background="@drawable/spinner_with_outline" />

        </LinearLayout>

    </LinearLayout>

    <!-- Announcements List -->

    <!-- Bottom Navigation Container (FrameLayout for floating button) -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefreshLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottomNavigationContainer"
        android:layout_below="@id/alerts_header_row"
        android:layout_marginTop="@dimen/margin_medium"
        android:layout_marginBottom="0dp">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/announcementsRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:padding="@dimen/padding_large" />
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <FrameLayout
        android:id="@+id/bottomNavigationContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="@dimen/dashboard_bottom_nav_height"
        android:paddingBottom="0dp"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:layout_alignParentBottom="true">

        <!-- Bottom Navigation Bar -->
        <LinearLayout
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dashboard_bottom_nav_height"
            android:orientation="horizontal"
            android:background="#FFFFFF"
            android:elevation="8dp"
            android:clipChildren="false">

            <!-- Home Tab -->
            <LinearLayout
                android:id="@+id/nav_home"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center"
                android:background="?android:attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true"
                android:minHeight="@dimen/dashboard_bottom_nav_height"
                android:minWidth="0dp">

                <!-- Icon Container (consistent size for all tabs) -->
                <FrameLayout
                    android:layout_width="@dimen/dashboard_bottom_nav_icon_size"
                    android:layout_height="@dimen/dashboard_bottom_nav_icon_size"
                    android:layout_gravity="center">

                    <ImageView
                        android:layout_width="@dimen/dashboard_bottom_nav_icon_size"
                        android:layout_height="@dimen/dashboard_bottom_nav_icon_size"
                        android:layout_gravity="center"
                        android:src="@drawable/frame__10_" />
                </FrameLayout>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Home"
                    android:textColor="#999999"
                    android:textSize="@dimen/dashboard_bottom_nav_text_size"
                    android:layout_marginTop="@dimen/margin_xxsmall"/>
            </LinearLayout>

            <!-- Chat Tab -->
            <LinearLayout
                android:id="@+id/nav_chat"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center"
                android:background="?android:attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true"
                android:minHeight="@dimen/dashboard_bottom_nav_height"
                android:minWidth="0dp">

                <!-- Icon Container (consistent size for all tabs) -->
                <FrameLayout
                    android:layout_width="@dimen/dashboard_bottom_nav_icon_size"
                    android:layout_height="@dimen/dashboard_bottom_nav_icon_size"
                    android:layout_gravity="center">

                    <ImageView
                        android:id="@+id/chat_icon_alerts"
                        android:layout_width="@dimen/dashboard_bottom_nav_icon_size"
                        android:layout_height="@dimen/dashboard_bottom_nav_icon_size"
                        android:layout_gravity="center"
                        android:src="@drawable/frame__6_"
                        android:contentDescription="Chat Icon" />

                    <!-- Chat Notification Badge - Positioned above the icon -->
                    <TextView
                        android:id="@+id/chat_badge_alerts"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:layout_gravity="top|center_horizontal"
                        android:layout_marginTop="-2dp"
                        android:background="@drawable/notification_badge"
                        android:gravity="center"
                        android:text=""
                        android:textColor="@android:color/white"
                        android:textSize="@dimen/text_size_tiny"
                        android:textStyle="bold"
                        android:minWidth="20dp"
                        android:minHeight="20dp"
                        android:visibility="gone" />
                </FrameLayout>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Chat"
                    android:textColor="#999999"
                    android:textSize="@dimen/dashboard_bottom_nav_text_size"
                    android:layout_marginTop="@dimen/margin_xxsmall"/>
            </LinearLayout>

            <!-- Empty space for Report button (same weight as other tabs) -->
            <View
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:minHeight="@dimen/dashboard_bottom_nav_height"
                android:minWidth="0dp"/>

            <!-- Map Tab -->
            <LinearLayout
                android:id="@+id/nav_map"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center"
                android:background="?android:attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true"
                android:minHeight="@dimen/dashboard_bottom_nav_height"
                android:minWidth="0dp">

                <!-- Icon Container (consistent size for all tabs) -->
                <FrameLayout
                    android:layout_width="@dimen/dashboard_bottom_nav_icon_size"
                    android:layout_height="@dimen/dashboard_bottom_nav_icon_size"
                    android:layout_gravity="center">

                    <ImageView
                        android:layout_width="@dimen/dashboard_bottom_nav_icon_size"
                        android:layout_height="@dimen/dashboard_bottom_nav_icon_size"
                        android:layout_gravity="center"
                        android:src="@drawable/frame__8_" />
                </FrameLayout>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Map"
                    android:textColor="#999999"
                    android:textSize="@dimen/dashboard_bottom_nav_text_size"
                    android:layout_marginTop="@dimen/margin_xxsmall"/>
            </LinearLayout>

            <!-- Alerts Tab -->
            <LinearLayout
                android:id="@+id/nav_alerts"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center"
                android:background="?android:attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true"
                android:minHeight="@dimen/dashboard_bottom_nav_height"
                android:minWidth="0dp">

                <!-- Icon Container (consistent size for all tabs) -->
                <FrameLayout
                    android:layout_width="@dimen/dashboard_bottom_nav_icon_size"
                    android:layout_height="@dimen/dashboard_bottom_nav_icon_size"
                    android:layout_gravity="center">

                    <ImageView
                        android:id="@+id/alerts_icon"
                        android:layout_width="@dimen/dashboard_bottom_nav_icon_size"
                        android:layout_height="@dimen/dashboard_bottom_nav_icon_size"
                        android:layout_gravity="center"
                        android:src="@drawable/ic_megaphone"
                        android:contentDescription="Alerts Icon" />

                    <!-- Notification Badge - Positioned above the icon -->
                    <TextView
                        android:id="@+id/alerts_badge"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:layout_gravity="top|center_horizontal"
                        android:layout_marginTop="-2dp"
                        android:background="@drawable/notification_badge"
                        android:gravity="center"
                        android:text=""
                        android:textColor="@android:color/white"
                        android:textSize="@dimen/text_size_tiny"
                        android:textStyle="bold"
                        android:minWidth="20dp"
                        android:minHeight="20dp"
                        android:visibility="gone" />
                </FrameLayout>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Alerts"
                    android:textColor="#FF5722"
                    android:textSize="@dimen/dashboard_bottom_nav_text_size"
                    android:layout_marginTop="@dimen/margin_xxsmall"/>
            </LinearLayout>
        </LinearLayout>

        <!-- Floating Circular Report Button (overlaps navigation bar) -->
        <FrameLayout
            android:id="@+id/nav_report"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="center_horizontal|top"
            android:layout_marginTop="-20dp"
            android:elevation="19dp"
            android:clickable="true"
            android:focusable="true"
            android:background="@drawable/report_tab_circular_selector"
            android:foreground="?android:attr/selectableItemBackgroundBorderless"
            android:clipToOutline="false"
            android:outlineProvider="none">

            <ImageView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:src="@drawable/emergencyreporticon" />
        </FrameLayout>
    </FrameLayout>

</RelativeLayout>
